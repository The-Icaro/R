# Workspace e Variáveis

# Limpar o Workspace = ctrl + l

# Agora que está limpo o ws, para saber as variáveis/objetos no ws
ls()

# Para remover uma variável/objeto 
rm(nome da variável)

# E para remover todos os objetos/variáveis
rm(list = ls())
