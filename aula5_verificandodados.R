# Verificando Dados

# Podemos fazer a verificação com a função class()
# Ou pelas funções is.

# is.tipo de dado( variavél )


w = 8.9
x = TRUE
y = 'FALSE'
z = 5

# is.numeric() = se é ponto flutuante (float)
# is.integer() = se é inteiro
# is.character() = se é 'texto'(strings)
# is.logical() = se é booleano
# is.double() = se é número de dupla precisão
# is.complex() = se é número complexo


# Essa função retorna - TRUE ou FALSE